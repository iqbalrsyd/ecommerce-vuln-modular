# Server
PORT=3000
NODE_ENV=development

# To exercise every scanner rule, fill these with values that match the
# classic vulnerable test fixtures. See the README for the exact strings.
#
# The shipped defaults in packages/shared/config/src/index.js are SAFE
# placeholders so the repo can be cloned and scanned without tripping
# GitHub's push protection. Override locally to reproduce the
# vulnerable code paths.

STRIPE_SECRET_KEY=
TEST_PAN_VISA=
TEST_PAN_MASTERCARD=
JWT_SECRET=
